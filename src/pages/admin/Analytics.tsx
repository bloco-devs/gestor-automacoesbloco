/**
 * /admin/analytics — página oficial.
 * Thin wrapper que apenas monta o módulo. Toda a lógica vive em
 * `src/modules/analytics`.
 */
export { AnalyticsPage as default } from "@/modules/analytics";
