# 70 — Performance Center

Rota: `/admin/performance`.

Gráfico de barras (Recharts) com Média/P95/P99 por camada: Render, Queries, Realtime, Workflow, Routing, Knowledge, IA e Mesh medido (derivado de `meshEventHistory()`).

Sem polling. Sem consultas duplicadas. Amostragem estável em memória.
